{
  "name": "parteuno",
  "version": "1.0.0",
  "description": "",
  "main": "algoritmo.js",
  "scripts": {
    "build": "tsc -p tsconfig.json & tsc -p tsconfig.algoritmo.json",
    "build:algo": "tsc -p tsconfig.algoritmo.json",
    "watch": "tsc -p tsconfig.json --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
